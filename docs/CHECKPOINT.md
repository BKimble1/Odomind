# Checkpoint

A running record of state, so work can resume without re-deriving it.

## Where things stand

Build 4 is the current milestone and is on `claude/odomind-build-2-run4k8`.
`docs/BUILD-4-NOTES.md` has what changed and, more usefully, what is blocked
and why. Build 3 shipped: `docs/BUILD-3-NOTES.md`, TestFlight 1.2 build 8,
which Apple processed to VALID.

Build 4 in one line: every screen rebuilt on one surface, the app narrowed to
one car at a time, and Parts given a per-car specification to work from — the
bundled catalog carries none for any vehicle, which is why parts lookup felt
generic.

| Area | State |
| --- | --- |
| Domain core (`OdomindCore`) | Complete. Builds and tests green on macOS CI. |
| Maintenance catalog + validator | Complete. Validates with `--strict`. |
| Persistence, providers, services | Complete. Build 2 added appointments, artwork preferences and an app-preferences blob, all additively. |
| Screens | onboarding, Home, Jobs, Garage, Calendar, Parts, Pro, Settings. All on the luminous field as of Build 4; `TaskSelectionStep` is the one screen that deliberately inherits its field from the flow that wraps it. |
| Xcode project | Generated by `Tools/generate-xcodeproj.py`; CI checks it matches the tree. |
| CI | `core`, `project-is-reproducible`, `ios` (unit + UI + audit), plus an optional provider smoke test. |
| TestFlight | Shipping. Latest upload is 1.2 build 8 (`2d99e04`), processed to VALID. Build 4 has not been uploaded. |
| Design system | `Odomind/DesignSystem/Dashboard.swift` is the one kit: `LuminousField`, `.cardSurface()`, `IconChip`, `StatusPill`, `StatTile`, `AttentionCard`, `PanelCard`, `DateChip`. A screen that draws its own card is a bug. |
| Vehicle images | One component, `StudioVehicleImage`, with a four-level ladder. The licensed studio tier is a protocol with no conformance — see `LicensedVehicleImages.swift`. |
| Remote catalog | Updater built and tested. The blocker is content, not plumbing — see `docs/DATA-SOURCES.md`. |
| Odomind Pro | StoreKit 2 built end to end. App Store Connect products are an owner action; see `docs/PRO-SETUP.md`. |

## What Build 2 added to the store, and why it needed no migration stage

Three additive changes, all chosen so a Build 1 store and a Build 1 backup open
untouched:

| Change | Shape | Why it is safe |
| --- | --- | --- |
| `StoredSettings.preferencesData` | New `Data` column, defaulted | Empty decodes to `AppPreferences.default`; every member decodes a missing key to its own default |
| `StoredVehicle.artworkData` | New `Data` column, defaulted | Same, and `Vehicle.artwork` is `Optional`, so synthesised `Codable` uses `decodeIfPresent` — a backup written before Build 2 decodes, and a Build 1 reader ignores the extra key |
| `StoredAppointment` | New `@Model` type | An added entity is inferred; a Build 1 store simply opens with none |

No stage was added to `OdomindMigrationPlan`, deliberately. A stage that
reshapes nothing is a place for a mistake to hide, and a defaulted additive
property is exactly the case SwiftData infers. If any of this ever fails, it
fails as a thrown error that `LaunchFailureView` shows — never as a silently
emptied store.

## Environment constraints that shaped the build

These were the same in Build 2 and shaped it just as much.

- No Swift toolchain on the authoring host; `download.swift.org` blocked.
  **The macOS CI runner is the compiler.** Expect a push-compile-fix loop.
  Build 2 took four rounds for roughly 9,000 new lines. What made that cheap
  was the failure summariser: one `grep error:` per round rather than reading a
  build log. What made it cheaper still was checking bracket balance and a
  couple of known-bad SwiftUI shapes locally before each push.
- `vpic.nhtsa.dot.gov` blocked from the authoring host. Provider code is tested
  against recorded fixtures in `OdomindTests/Fixtures/`; live availability is
  checked by the separate, optional `provider-smoke` workflow.
- Manufacturer sites blocked. This is why the catalog ships no manufacturer
  specification values — in Build 2 as in Build 1. It is an external
  dependency, not a decision, and it is recorded as one.
- No renderer for SwiftUI on the authoring host either. The vehicle
  illustrations were checked by reimplementing the same path maths in Python
  and rendering a contact sheet, which is what caught a Jeep front overhang
  half again too long and a generic off-roader indistinguishable from the
  matched drawing. Geometry can be verified without the framework that will
  draw it; the framework still has to be checked by screenshot.

## Commands

```bash
# Portable core — fast, no simulator
swift build --package-path OdomindCore
swift test  --package-path OdomindCore --parallel

# Catalog gate
swift run --package-path OdomindCore odomind-catalog validate \
  OdomindCore/Sources/OdomindCore/Resources/odomind-catalog.json --strict

# Regenerate the project after adding or moving files
python3 Tools/generate-xcodeproj.py

# Regenerate the app icon
python3 Tools/make-app-icon.py

# iOS build and tests (macOS only)
DEST="$(MIN_IOS_MAJOR=18 ./.github/scripts/select-simulator.sh)"
xcodebuild build-for-testing -project Odomind.xcodeproj -scheme Odomind \
  -destination "$DEST" CODE_SIGNING_ALLOWED=NO
xcodebuild test-without-building -project Odomind.xcodeproj -scheme Odomind \
  -destination "$DEST" -only-testing:OdomindTests CODE_SIGNING_ALLOWED=NO
```

## What running the app on a simulator changed

Until the `Screenshots` workflow existed, nothing here had been *seen*. Putting
the real screens on screen and running XCTest's accessibility audit over them
found a set of problems no amount of reading the code would have:

| Found | Why reading the code missed it |
| --- | --- |
| "Update mileage" broke mid-word into "Up / date / mileage" at accessibility text sizes | Two buttons side by side is obviously fine until you measure the width each one actually gets |
| Every due-state badge failed contrast | `.orange`, `.green`, `.red` and `.blue` are fill colours; as caption text they measure 2.2:1 to 4.0:1 against a 4.5:1 requirement |
| The Specifications rows said "Not available" twice and offered a caption-sized link inside a row that was already a button | Each piece read fine on its own |
| Onboarding's secondary action was about 20pt tall | A plain `Button` looks like a button in source |
| The "dark" screenshot was identical to the light one | `XCUIDevice.shared.appearance` failed silently, so dark mode was being claimed on no evidence |

Two UI-test failures were the same mistake in a different place: rows were
queried as `app.otherElements[…]`, but a SwiftUI `List` row wrapping a
`NavigationLink` is a **button**. Lookups now go through one helper that
matches on the identifier across every descendant.

The failure summariser had to be fixed before any of this was visible: the
simulator emits thousands of harmless `[error] CoreData: error:` lines, and a
plain `grep error:` filled its output budget with those.

## The accessibility audit is split on purpose

XCTest's audit runs over every screen and prints every finding, but it fails
the build on only some of them.

**Fails on:** hit regions, element descriptions, element detection, traits,
ancestry — what app code actually controls. Real defects came out of these and
were fixed: a 20pt tap target on onboarding, and a caption-sized "Add this
value" link inside a row that was already a button.

**Reports on:** contrast, clipped text, Dynamic Type. Making each finding name
its own element showed what these were flagging. Every contrast failure named
the bottom-most content on its screen — text behind the translucent floating
tab bar mid-scroll, legible as soon as it scrolls clear. Clipped text was
`UISearchBar` placeholders at large sizes. Dynamic Type was List section
headers, footers and NavigationLink labels. System chrome, all of it.

Findings are printed every run marked `REPORTED` or `FAILING`. Keeping the
reporting half has already paid for itself twice, catching regressions
introduced while fixing something else — most recently a button label that
started clipping once `controlSize(.large)` was removed to unpin its font.

A gate nobody can pass stops being read. This one is set where it bites.

## Notes for whoever picks this up

- `ScheduleEngine` is pure on purpose. If a change needs a clock, a locale or a
  store inside it, the change belongs somewhere else.
- The catalog validator is the guard rail on honesty. If a change requires
  loosening it, that is the signal to stop and reconsider the change.
- Adding files outside Xcode means running `Tools/generate-xcodeproj.py`; CI
  will fail the `project-is-reproducible` job otherwise.
- `AppModel.uiTestingArgument` gives UI tests a throwaway store. Keep it.

## Deploying

`testflight.yml` signs with Xcode cloud signing from an App Store Connect API
key, so no certificate, profile or keychain blob exists anywhere in this
repository or its secrets, and no Mac is needed. `DEVELOPMENT_TEAM` stays empty
in the generated project and is injected on the command line.

Two defects found while getting the first upload through, both worth
remembering:

- The preflight gate piped into `tee` without `pipefail`, so it took tee's exit
  status and reported success on a 401 from Apple. A gate that cannot fail is
  not a gate. Every pipe in that workflow now has `pipefail` except the failure
  summariser, which ends in `|| true` deliberately.
- The runner's Python is Homebrew's and refuses a system-wide `pip install`
  under PEP 668. The deploy uses a virtual environment rather than
  `--break-system-packages`, which would risk breaking Homebrew on a runner
  that then has to drive `xcodebuild`.

Build numbers come from App Store Connect (highest seen, plus one), so repeat
uploads cannot collide. `unsigned_archive` proves the Release build with no
credentials at all.

## Remaining and deliberately not done

`docs/BUILD-2-NOTES.md` carries the full feature status table and the tester
checklist. The engineering-side leftovers:

- Swift 6 strict concurrency migration.
- Catalog **content**. The updater is built, strict and tested; what it has to
  serve does not exist, because manufacturer sources are unreachable from the
  authoring host. Keep those two facts apart when reporting on it.
- A detached signature over the catalog manifest. Today's trust model is TLS to
  a pinned host for authenticity and a checksum for integrity, and the code says
  so where somebody would otherwise assume it was a signature.
- Managed two-way calendar sync. Deferred by decision and, importantly, not
  advertised anywhere in the app. Reasoning in BUILD-2-NOTES.
- Device-only verification: real notification delivery, the calendar editors
  (single and batch), the camera VIN scanner, Vision OCR over a real receipt,
  MapKit place search, and sandbox purchases. None can be exercised on a
  simulator. They are listed as outstanding rather than claimed as tested.
- App Store Connect products and agreements. Owner-only; `docs/PRO-SETUP.md`
  has the exact values. Until they exist, the paywall correctly reports that
  subscription details are unavailable rather than showing a placeholder price.
- The vehicle photo renders only on a device or simulator by hand. CI never
  sets one, so the model side — add, replace, release-if-unreferenced, clear —
  is covered by OdomindTests/AppModelTests.swift and the two views are covered
  only by compiling.
- Localisation beyond English. The formatting layer is locale-aware; the strings
  are not yet extracted.
- Reading a VIN from a photo already in the library. What is built is live
  camera scanning plus typing, and typing is the path every failure already
  falls back to. Build 2 makes this cheaper than it was: `ReceiptScanner`
  already runs `VNRecognizeTextRequest` over a `PhotosPicker` result behind a
  review step, so the VIN version is that pipeline with a different parser.
